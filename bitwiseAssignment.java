import java.lang.Math;
public class bitwiseAssignment {
    public static void main(String args[]){        
        //Swap 2-no. without using 3rd value
        // int x = 3, y = 5;
        // System.out.println("Before Swap:-" + x+ " " + y);
        // x = x ^ y;
        // y = x ^ y;
        // x = x ^ y;
        // System.out.println("After Swap:-" +x+ " " +y);
    }
}
